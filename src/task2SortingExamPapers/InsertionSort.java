package task2SortingExamPapers;

public class InsertionSort {
	StudentListSort studentlist;
	public InsertionSort(StudentListSort sls) {
		studentlist = new StudentListSort();
		for(Student s:sls.getStudentList()) {
			studentlist.
		}
	}
}
